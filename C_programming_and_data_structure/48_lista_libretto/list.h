typedef struct counter_nodes* head;
typedef struct node* list;

struct counter_nodes* newHeadList(  );
struct node* newTopList(  );
struct counter_nodes* addingItem( head );
void printList( head );
int searchList( head, char *);
